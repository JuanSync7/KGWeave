# Python module notes

The `add` function lives in `simple_module`; it sums two ints.

The `Counter` class exposes a `bump` method.

```python
from simple_module import add, Counter
c = Counter()
c.bump()
```

Also referenced but should not match: `not_a_real_symbol`.
