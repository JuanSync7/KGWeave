# v1.6 #3 — pre-flight audit of the libcst Python walker

HEAD: `0ad4efe` (after v1.6-#4). Walker:
`src/knowledge_graph/builders/py/walker.py` (200 LOC), kinds emitted
today: `PyModule`, `PyImport`, `PyFunction`, `PyClass` only.

Tests under `tests/knowledge_graph/builders/py/` cover only that v1 set
(`test_walker.py:19-99`). No fixtures or assertions exist for any of
the 9 charter gaps.

| # | kind                  | status        | evidence                                                                                  |
|---|-----------------------|---------------|-------------------------------------------------------------------------------------------|
| 1 | decorators            | needs work    | walker has no `decorators` handling; no test mentions `decorator`                         |
| 2 | async def             | needs work    | `_emit_def_or_class` only checks `cst.FunctionDef`; no `is_async` flag                    |
| 3 | PEP 695 type alias    | needs work    | walker skips `SimpleStatementLine` non-imports; no `PyTypeAlias` kind                     |
| 4 | TYPE_CHECKING imports | needs work    | walker walks top-level `if` is "intentionally skipped" (walker.py:194-195)                |
| 5 | `__all__` exports     | needs work    | walker never inspects `Assign`; PyModule payload is `{}` (walker.py:115)                  |
| 6 | `.pyi` stub files     | needs work    | facade dispatch is by `source=` not extension; lift_python is content-only — easy add     |
| 7 | match-case            | needs work    | top-level `Match` falls through the else branch at walker.py:194-195                      |
| 8 | walrus                | needs work    | walker does not visit expressions / assignments at all                                    |
| 9 | comprehension scopes  | needs work    | walker only recurses into FunctionDef/ClassDef bodies (walker.py:145-150)                 |

All 9 require work. Planned order = charter order (1→9), each as a
red+green commit pair: fixture + walker test + writer round-trip test
RED first, then walker change GREEN. After each kind a dir-scoped
`pytest tests/knowledge_graph/builders/py/ -v` sanity run before
moving on.
