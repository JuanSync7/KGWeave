// Empty stub for slang elaboration of OpenTitan DV files.
// KGWeave does not validate UVM behavior — it only needs the symbols to
// resolve so covergroup definitions are reachable. Replace with the real
// UVM package if running a UVM-aware compile.
package uvm_pkg;
endpackage
